const multer=require('multer');const path=require('path');const fs=require('fs');function makeStorage(subfolder){const dest=path.join(__dirname,'..','uploads',subfolder);if(!fs.existsSync(dest))fs.mkdirSync(dest,{recursive:true});return multer.diskStorage({destination:(_req,_file,cb)=>cb(null,dest),filename:(_req,file,cb)=>{const safeExt=path.extname(file.originalname).slice(0,10);const unique=`${Date.now()}-${Math.round(Math.random()*1e9)}${safeExt}`;cb(null,unique)}})}const kycUpload=multer({storage:makeStorage('kyc'),limits:{fileSize:10*1024*1024},fileFilter:(_req,file,cb)=>{const ok=/^image\/(png|jpe?g|webp)$|^application\/pdf$/.test(file.mimetype);cb(ok?null:new Error('Only JPG, PNG, WEBP images or PDF files are allowed for KYC documents.'),ok)}});const contentUpload=multer({storage:makeStorage('content'),limits:{fileSize:200*1024*1024},fileFilter:(_req,file,cb)=>{const ok=/^image\/(png|jpe?g|webp|gif)$|^video\/(mp4|webm|quicktime)$|^application\/pdf$/.test(file.mimetype);cb(ok?null:new Error('Unsupported file type. Allowed: images, mp4/webm/mov video, or PDF.'),ok)}});module.exports={kycUpload,contentUpload};
